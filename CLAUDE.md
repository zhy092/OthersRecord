# CLAUDE.md


本文件为Claude Code (claude.ai/code) 在处理此代码库时提供指导。

## 开发命令

- **启动开发服务器**: `npm run dev`
- **构建生产资源**: `npm run build`
- **运行代码检查**: `npm run lint`（根据 DESIGN.md 指南验证）
- **运行测试**: `npm test` 或 `npm test -- <文件名>` 以运行特定测试

## 架构概述

### 设计系统实现
- **规范来源**: `DESIGN.md` 包含完整的视觉规范（颜色、排版、组件）
- **颜色系统**: 在 `src/theme/colors.css` 中实现为 CSS 变量，使用 DESIGN.md 中的精确色板
- **排版**: Tailwind 配置已扩展以匹配 DESIGN.md 中的字号比例（第33-41行）
- **组件结构**: 所有UI组件遵循 DESIGN.md 规范（第43-66行），采用 PascalCase 命名

### 关键目录
- `src/components/` - 所有UI组件（Button、Card、Input等）
- `src/theme/` - 设计令牌、间距比例（第68-81行）和颜色定义
- `src/pages/` - 使用组件库的页面级布局

## 特别注意事项

- **命名规范**: 组件文件使用 PascalCase（例如 `Button.jsx`、`Card.jsx`）
- **间距使用**: 引用间距比例（第68-81行）— 使用 `spacing-4` 而非硬编码的 `16px`
- **颜色使用**: 始终引用设计令牌（例如 `clawOrange`）而非直接使用十六进制值
- **设计合规**: 在实施UI更改前，需对照 DESIGN.md 的相关章节进行验证
- **无障碍**: 所有组件必须符合 DESIGN.md 中引用的 WCAG 2.1 AA 标准（第164行）


行为准则：减少常见LLM编码错误。可根据项目具体需求合并补充。

**权衡点**：这些准则偏向谨慎而非速度。简单任务请自行判断适用性。

## 1. 编码前思考

**不要假设。不要隐藏困惑。明确权衡点。**

实现前：
- 明确陈述你的假设。如有不确定，必须询问。
- 存在多种可能解释时，应全部呈现 - 不要自行选择。
- 发现更简单方案时应提出建议。必要时应提出反对意见。
- 遇到不明确处立即停止。指出具体困惑点。必须询问。

## 2. 简洁优先

**实现问题的最小代码量。不包含推测性内容。**

- 不添加超出要求的功能
- 不为单次使用代码创建抽象
- 不添加未明确要求的"灵活性"或"可配置性"
- 不处理不可能发生的错误场景
- 若200行代码可简化为50行，必须重写

自问："资深工程师会认为这过于复杂吗？" 若答案是肯定的，必须简化。

## 3. 精准修改

**仅修改必要部分。只清理自己造成的混乱。**

修改现有代码时：
- 不要"改进"相邻的无关代码、注释或格式
- 不重构未损坏的代码
- 遵循现有代码风格，即使与个人习惯不同
- 如发现无关的废弃代码，应指出但不要删除

当你的修改产生废弃内容时：
- 仅删除**本次修改导致**不再使用的导入/变量/函数
- 除非被明确要求，否则不要删除预先存在的废弃代码

检验标准：每个修改的代码行都必须直接关联到用户请求。

## 4. 目标驱动执行

**定义成功标准。持续验证直至达成。**

将任务转化为可验证目标：
- "添加验证" → "编写无效输入测试→使测试通过"
- "修复bug" → "编写复现测试→使测试通过"
- "重构X" → "确保重构前后测试均通过"

多步骤任务需声明简要计划：
```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

明确的成功标准使你能独立验证。模糊标准（"使其工作"）需要不断澄清。

---

**这些准则有效的标志是**：
diff中不必要的更改减少，因过度复杂导致的重写减少，澄清问题在实现前而非错误后出现。